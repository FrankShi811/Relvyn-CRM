<UserControl x:Class="WAFlow.Desktop.Pages.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="CustomerCellText" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="NoWrap"/><Setter Property="TextTrimming" Value="CharacterEllipsis"/><Setter Property="VerticalAlignment" Value="Center"/><Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}"/>
        </Style>
        <Style x:Key="CustomerScrollPageButton" TargetType="{x:Type RepeatButton}">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border Background="{TemplateBinding Background}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CustomerScrollThumb" TargetType="{x:Type Thumb}">
            <Setter Property="MinWidth" Value="8"/>
            <Setter Property="MinHeight" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border Background="{DynamicResource LineStrong}" CornerRadius="5" Margin="3"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CustomerScrollBar" TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="{DynamicResource SurfaceMuted}"/>
            <Setter Property="Width" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Style="{StaticResource CustomerScrollPageButton}"/></Track.DecreaseRepeatButton>
                                <Track.Thumb><Thumb Style="{StaticResource CustomerScrollThumb}"/></Track.Thumb>
                                <Track.IncreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Style="{StaticResource CustomerScrollPageButton}"/></Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="14"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                <Grid Background="{TemplateBinding Background}">
                                    <Track x:Name="PART_Track">
                                        <Track.DecreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}" Style="{StaticResource CustomerScrollPageButton}"/></Track.DecreaseRepeatButton>
                                        <Track.Thumb><Thumb Style="{StaticResource CustomerScrollThumb}"/></Track.Thumb>
                                        <Track.IncreaseRepeatButton><RepeatButton Command="{x:Static ScrollBar.PageRightCommand}" Style="{StaticResource CustomerScrollPageButton}"/></Track.IncreaseRepeatButton>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid Margin="30,26,30,28">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="UNIFIED CUSTOMER GRAPH" Style="{StaticResource PageEyebrow}"/>
                <TextBlock Text="每一列，都是客户上下文" Style="{StaticResource PageTitle}" Margin="0,5,0,0"/>
                <TextBlock Text="保留上传表格的全部动态维度；搜索、筛选、编辑与 WhatsApp 侧栏使用同一份客户数据。" Foreground="{DynamicResource Muted}" Margin="0,6,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="EditButton" Content="编辑客户" Style="{StaticResource SecondaryButton}" Click="Edit_Click" IsEnabled="False" Margin="0,0,8,0"/>
                <Button x:Name="DeleteSelectedButton" Content="删除所选" Style="{StaticResource DangerButton}" Click="DeleteSelected_Click" IsEnabled="False" Margin="0,0,8,0"/>
                <Button Content="＋  导入客户数据" Style="{StaticResource PrimaryButton}" Click="Import_Click"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="14" Margin="0,19,0,14">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="120"/><ColumnDefinition Width="145"/><ColumnDefinition Width="165"/><ColumnDefinition Width="1.05*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <Grid>
                    <TextBlock Text="⌕" FontSize="16" Foreground="{DynamicResource Muted}" VerticalAlignment="Center" Margin="11,0,0,0" Panel.ZIndex="1" IsHitTestVisible="False"/>
                    <TextBox x:Name="SearchBox" ToolTip="搜索所有核心字段与原表维度" Padding="34,8,11,8" Margin="0,0,10,0" KeyDown="SearchBox_KeyDown"/>
                </Grid>
                <ComboBox x:Name="GradeFilter" Grid.Column="1" Margin="0,0,10,0" SelectionChanged="Filter_SelectionChanged"/>
                <ComboBox x:Name="StageFilter" Grid.Column="2" Margin="0,0,10,0" DisplayMemberPath="Label" SelectionChanged="Filter_SelectionChanged"/>
                <ComboBox x:Name="CategoryPreferenceFilter" Grid.Column="3" DisplayMemberPath="Label" ToolTip="按一级品类偏好筛选客户" Margin="0,0,10,0" SelectionChanged="Filter_SelectionChanged"/>
                <ComboBox x:Name="CustomFieldFilter" Grid.Column="4" ToolTip="选择要显示的原始表格维度列" Margin="0,0,10,0" SelectionChanged="Filter_SelectionChanged"/>
                <Button Grid.Column="5" Content="清除筛选" Style="{StaticResource SecondaryButton}" Click="Clear_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <Border Background="{DynamicResource SurfaceMuted}" BorderBrush="{DynamicResource Line}" BorderThickness="0,0,0,1" Padding="16,11" CornerRadius="16,16,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Background="{DynamicResource PrimarySoft}" CornerRadius="8" Padding="8,4" Margin="0,0,10,0"><TextBlock Text="CRM DATA" Foreground="{DynamicResource Primary}" FontSize="9" FontWeight="Bold"/></Border>
                            <TextBlock x:Name="ListStatsText" Foreground="{DynamicResource Muted}" TextWrapping="NoWrap" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <CheckBox x:Name="SelectAllCheckBox" Content="全选本页" IsThreeState="True" Click="SelectAll_Click" VerticalAlignment="Center" Margin="0,0,14,0"/>
                            <Border Background="{DynamicResource AiSoft}" CornerRadius="9" Padding="9,5" Margin="0,0,9,0"><TextBlock x:Name="SelectedCountText" Text="已选 0 位" Foreground="{DynamicResource AiAccentDeep}" FontWeight="SemiBold" FontSize="10.5"/></Border>
                            <Button x:Name="ClearSelectionButton" Content="取消选择" Style="{StaticResource GhostButton}" Click="ClearSelection_Click" IsEnabled="False" Padding="9,5" MinHeight="28"/>
                        </StackPanel>
                    </Grid>
                </Border>
                <DataGrid x:Name="CustomerGrid" Grid.Row="1" IsReadOnly="True" EnableRowVirtualization="True" EnableColumnVirtualization="True"
                          ScrollViewer.CanContentScroll="True" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto"
                          FrozenColumnCount="0" CanUserSortColumns="True" Sorting="CustomerGrid_Sorting"
                          SelectionChanged="CustomerGrid_SelectionChanged" MouseDoubleClick="CustomerGrid_MouseDoubleClick" BorderThickness="0">
                    <DataGrid.Resources>
                        <Style TargetType="{x:Type ScrollBar}" BasedOn="{StaticResource CustomerScrollBar}"/>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="" Width="46" CanUserSort="False"><DataGridTemplateColumn.CellTemplate><DataTemplate><CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" VerticalAlignment="Center" ToolTip="选择该客户"/></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn>
                        <DataGridTextColumn Header="客户" Binding="{Binding DisplayName}" SortMemberPath="DisplayName" Width="190" ElementStyle="{StaticResource CustomerCellText}"/>
                        <DataGridTextColumn Header="客户 ID" Binding="{Binding BuyerId}" SortMemberPath="BuyerId" Width="145" ElementStyle="{StaticResource CustomerCellText}"/>
                        <DataGridTextColumn Header="公司" Binding="{Binding Company}" SortMemberPath="Company" Width="175" ElementStyle="{StaticResource CustomerCellText}"/>
                        <DataGridTextColumn Header="邮箱" Binding="{Binding Email}" SortMemberPath="Email" Width="215" ElementStyle="{StaticResource CustomerCellText}"/>
                        <DataGridTextColumn Header="国家" Binding="{Binding Country}" SortMemberPath="Country" Width="125" ElementStyle="{StaticResource CustomerCellText}"/>
                        <DataGridTextColumn Header="WhatsApp" Binding="{Binding PhoneE164}" SortMemberPath="PhoneE164" Width="150" ElementStyle="{StaticResource CustomerCellText}"/>
                        <DataGridTextColumn Header="WhatsApp 状态" Binding="{Binding PhoneState}" SortMemberPath="PhoneState" Width="128" ElementStyle="{StaticResource CustomerCellText}"/>
                        <DataGridTextColumn Header="标签" Binding="{Binding TagsLabel}" SortMemberPath="TagsLabel" Width="190" ElementStyle="{StaticResource CustomerCellText}"/>
                        <DataGridTextColumn Header="负责人" Binding="{Binding Owner}" SortMemberPath="Owner" Width="120" ElementStyle="{StaticResource CustomerCellText}"/>
                        <DataGridTextColumn Header="等级" Binding="{Binding Grade}" SortMemberPath="Grade" Width="64" ElementStyle="{StaticResource CustomerCellText}"/>
                        <DataGridTextColumn Header="阶段" Binding="{Binding StageLabel}" SortMemberPath="Stage" Width="92" ElementStyle="{StaticResource CustomerCellText}"/>
                        <DataGridTextColumn Header="一级品类偏好" Binding="{Binding PrimaryCategoryPreference}" SortMemberPath="PrimaryCategoryPreference" Width="180" ElementStyle="{StaticResource CustomerCellText}"/>
                    </DataGrid.Columns>
                </DataGrid>
                <Border Grid.Row="2" Background="{DynamicResource SurfaceMuted}" BorderBrush="{DynamicResource Line}" BorderThickness="0,1,0,0" Padding="16,9" CornerRadius="0,0,16,16">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="每页" Foreground="{DynamicResource Muted}" VerticalAlignment="Center" Margin="0,0,7,0"/>
                            <ComboBox x:Name="PageSizeBox" Width="112" DisplayMemberPath="Label" SelectionChanged="PageSizeBox_SelectionChanged"/>
                            <TextBlock x:Name="PageRangeText" Foreground="{DynamicResource Muted}" VerticalAlignment="Center" Margin="12,0,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button x:Name="PreviousPageButton" Content="上一页" Style="{StaticResource SecondaryButton}" Click="PreviousPage_Click" Padding="12,6" MinHeight="30"/>
                            <TextBlock x:Name="PageStatusText" Foreground="{DynamicResource Ink}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="13,0"/>
                            <Button x:Name="NextPageButton" Content="下一页" Style="{StaticResource SecondaryButton}" Click="NextPage_Click" Padding="12,6" MinHeight="30"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
