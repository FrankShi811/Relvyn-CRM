<Application x:Class="WAFlow.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WAFlow.Desktop"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <local:ComboSelectionTextConverter x:Key="ComboSelectionTextConverter"/>
        <!-- AI Sales OS 2.x semantic design tokens. ThemeManager mutates these shared brushes in-place. -->
        <SolidColorBrush x:Key="Ink" Color="#15251E"/>
        <SolidColorBrush x:Key="InkSecondary" Color="#43564D"/>
        <SolidColorBrush x:Key="Muted" Color="#586B64"/>
        <SolidColorBrush x:Key="MutedSubtle" Color="#96A8A1"/>
        <SolidColorBrush x:Key="Primary" Color="#087A59"/>
        <SolidColorBrush x:Key="PrimaryDark" Color="#066A4D"/>
        <SolidColorBrush x:Key="PrimaryHover" Color="#066A4D"/>
        <SolidColorBrush x:Key="OnPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="PrimarySoft" Color="#D9F5EB"/>
        <SolidColorBrush x:Key="PrimarySurface" Color="#ECFAF5"/>
        <SolidColorBrush x:Key="AiAccent" Color="#6659B8"/>
        <SolidColorBrush x:Key="AiAccentDeep" Color="#51459F"/>
        <SolidColorBrush x:Key="OnAi" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="AiProcessing" Color="#31C8E5"/>
        <SolidColorBrush x:Key="AiSoft" Color="#E8E3FF"/>
        <SolidColorBrush x:Key="AiSurface" Color="#F4F1FF"/>
        <SolidColorBrush x:Key="Surface" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SurfaceElevated" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SurfaceMuted" Color="#F4F7F5"/>
        <SolidColorBrush x:Key="SurfaceInput" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Canvas" Color="#F4F7F5"/>
        <SolidColorBrush x:Key="CanvasDeep" Color="#E8EEEB"/>
        <SolidColorBrush x:Key="Line" Color="#DDE5E1"/>
        <SolidColorBrush x:Key="LineStrong" Color="#B9C9C3"/>
        <SolidColorBrush x:Key="Sidebar" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SidebarElevated" Color="#F1F6F3"/>
        <SolidColorBrush x:Key="SidebarHover" Color="#EAF4EF"/>
        <SolidColorBrush x:Key="SidebarActive" Color="#E1F1EA"/>
        <SolidColorBrush x:Key="SidebarText" Color="#24362F"/>
        <SolidColorBrush x:Key="SidebarMuted" Color="#586B64"/>
        <SolidColorBrush x:Key="LogoSurface" Color="#EEF2F0"/>
        <SolidColorBrush x:Key="LogoBorder" Color="#D6DEDA"/>
        <SolidColorBrush x:Key="UnreadBadgeBackground" Color="#C43131"/>
        <SolidColorBrush x:Key="UnreadBadgeText" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Success" Color="#16B889"/>
        <SolidColorBrush x:Key="SuccessSoft" Color="#E0F7EF"/>
        <SolidColorBrush x:Key="Warning" Color="#E0A12B"/>
        <SolidColorBrush x:Key="WarningSoft" Color="#FFF2D6"/>
        <SolidColorBrush x:Key="Danger" Color="#E35D5D"/>
        <SolidColorBrush x:Key="DangerSoft" Color="#FDE7E7"/>
        <SolidColorBrush x:Key="OnDanger" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Info" Color="#4E8CF7"/>
        <SolidColorBrush x:Key="InfoSoft" Color="#E9F1FF"/>
        <SolidColorBrush x:Key="GradeA" Color="#16B889"/>
        <SolidColorBrush x:Key="GradeB" Color="#4E8CF7"/>
        <SolidColorBrush x:Key="GradeC" Color="#E0A12B"/>
        <SolidColorBrush x:Key="GradeD" Color="#83958E"/>
        <SolidColorBrush x:Key="ChatOutbound" Color="#D1F5E8"/>
        <SolidColorBrush x:Key="ChatInbound" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Overlay" Color="#B80A1813"/>
        <SolidColorBrush x:Key="GlassSurface" Color="#EFFFFFFF"/>
        <SolidColorBrush x:Key="GlassSurfaceStrong" Color="#F8FFFFFF"/>
        <SolidColorBrush x:Key="GlassLine" Color="#90D9E0DD"/>

        <!-- Stitch/Auralis-inspired ambient light: restrained, static, and GPU-cheap. -->
        <LinearGradientBrush x:Key="AuroraAmbient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F8FFFFFF" Offset="0"/>
            <GradientStop Color="#EFF2EEFF" Offset="0.42"/>
            <GradientStop Color="#E8EAF9FF" Offset="0.68"/>
            <GradientStop Color="#E7E6FAF3" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="AuroraBorder" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#55FFFFFF" Offset="0"/>
            <GradientStop Color="#807868FF" Offset="0.52"/>
            <GradientStop Color="#5031C8E5" Offset="1"/>
        </LinearGradientBrush>

        <DropShadowEffect x:Key="CardShadow" Color="#18342B" BlurRadius="22" ShadowDepth="3" Opacity="0.09"/>
        <DropShadowEffect x:Key="FloatingShadow" Color="#102A21" BlurRadius="34" ShadowDepth="9" Opacity="0.16"/>
        <DropShadowEffect x:Key="AiGlow" Color="#7868FF" BlurRadius="26" ShadowDepth="0" Opacity="0.18"/>
        <DropShadowEffect x:Key="SidebarOverlayShadow" Color="#07100D" BlurRadius="28" ShadowDepth="8" Direction="0" Opacity="0.18"/>

        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI, Microsoft YaHei UI"/>
            <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
            <Setter Property="Background" Value="{DynamicResource Canvas}"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
        </Style>
        <Style TargetType="UserControl">
            <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
        </Style>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
        <Style TargetType="{x:Type Label}">
            <Setter Property="Foreground" Value="{DynamicResource InkSecondary}"/>
        </Style>
        <Style TargetType="{x:Type RadioButton}">
            <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
        </Style>
        <Style TargetType="{x:Type GroupBox}">
            <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
        </Style>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Surface}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Line}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
        </Style>
        <Style x:Key="ElevatedCard" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="Background" Value="{DynamicResource SurfaceElevated}"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style x:Key="GlassCard" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="Background" Value="{DynamicResource GlassSurface}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource GlassLine}"/>
            <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
        </Style>
        <Style x:Key="AmbientHeroCard" TargetType="Border" BasedOn="{StaticResource GlassCard}">
            <Setter Property="Background" Value="{DynamicResource AuroraAmbient}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AuroraBorder}"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="24,21"/>
        </Style>
        <Style x:Key="MetricCard" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="Padding" Value="18,16"/>
            <Setter Property="CornerRadius" Value="11"/>
        </Style>
        <Style x:Key="ElevatedMetricCard" TargetType="Border" BasedOn="{StaticResource GlassCard}">
            <Setter Property="Padding" Value="19,17"/>
            <Setter Property="CornerRadius" Value="12"/>
        </Style>
        <Style x:Key="AiCard" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="Background" Value="{DynamicResource AiSurface}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AiSoft}"/>
            <Setter Property="Effect" Value="{StaticResource AiGlow}"/>
        </Style>
        <Style x:Key="IntelligenceGlassCard" TargetType="Border" BasedOn="{StaticResource AiCard}">
            <Setter Property="Background" Value="{DynamicResource AuroraAmbient}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AuroraBorder}"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Effect" Value="{StaticResource AiGlow}"/>
        </Style>
        <Style x:Key="HolographicCard" TargetType="Border" BasedOn="{StaticResource AiCard}">
            <Setter Property="BorderBrush" Value="{DynamicResource AiAccent}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="13"/>
            <Setter Property="Padding" Value="22"/>
        </Style>
        <Style x:Key="ReasoningStepCard" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="Background" Value="{DynamicResource SurfaceMuted}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AiSoft}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="14,12"/>
        </Style>
        <Style x:Key="PriorityCard" TargetType="Border" BasedOn="{StaticResource ElevatedCard}">
            <Setter Property="BorderBrush" Value="{DynamicResource PrimarySoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
        </Style>
        <Style x:Key="WorkflowNodeCard" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="16,14"/>
            <Setter Property="BorderBrush" Value="{DynamicResource LineStrong}"/>
        </Style>
        <Style x:Key="InboundMessageBubble" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ChatInbound}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Line}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14,14,14,4"/>
            <Setter Property="Padding" Value="13,10"/>
        </Style>
        <Style x:Key="OutboundMessageBubble" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ChatOutbound}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PrimarySoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14,14,4,14"/>
            <Setter Property="Padding" Value="13,10"/>
        </Style>

        <Style x:Key="ButtonBase" TargetType="Button">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="17,9"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="local:MotionAssist.IsEnabled" Value="True"/>
            <Setter Property="local:MotionAssist.HoverScale" Value="1.012"/>
            <Setter Property="local:MotionAssist.PressedScale" Value="0.975"/>
            <Setter Property="local:MotionAssist.HoverOffsetY" Value="-1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="{Binding Foreground, RelativeSource={RelativeSource TemplatedParent}}">
                                <ContentPresenter.Resources>
                                    <Style TargetType="{x:Type TextBlock}">
                                        <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"/>
                                    </Style>
                                    <Style TargetType="{x:Type AccessText}">
                                        <Setter Property="TextElement.Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource AiAccent}"/><Setter TargetName="Root" Property="BorderThickness" Value="2"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Root" Property="Opacity" Value="0.40"/><Setter Property="Cursor" Value="Arrow"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ButtonBase}">
            <Setter Property="Background" Value="{DynamicResource Primary}"/>
            <Setter Property="Foreground" Value="{DynamicResource OnPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Primary}"/>
        </Style>
        <Style x:Key="AiButton" TargetType="Button" BasedOn="{StaticResource ButtonBase}">
            <Setter Property="Background" Value="{DynamicResource AiAccent}"/>
            <Setter Property="Foreground" Value="{DynamicResource OnAi}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AiAccent}"/>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ButtonBase}">
            <Setter Property="Background" Value="{DynamicResource SurfaceElevated}"/>
            <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource LineStrong}"/>
        </Style>
        <Style x:Key="GhostButton" TargetType="Button" BasedOn="{StaticResource ButtonBase}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource InkSecondary}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
        </Style>
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ButtonBase}">
            <Setter Property="Background" Value="{DynamicResource Danger}"/>
            <Setter Property="Foreground" Value="{DynamicResource OnDanger}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Danger}"/>
        </Style>
        <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="15"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceInput}"/>
            <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource Primary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource LineStrong}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="11,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="SelectionBrush" Value="{DynamicResource PrimarySoft}"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter Property="BorderBrush" Value="{DynamicResource Primary}"/></Trigger>
                <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.45"/></Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceInput}"/>
            <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource LineStrong}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="11,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="MinHeight" Value="40"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource SurfaceInput}"/>
            <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource LineStrong}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="9,7"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="DropDownToggle" Focusable="False" ClickMode="Press"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="ComboBorder" Background="{DynamicResource SurfaceInput}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="10,0">
                                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="20"/></Grid.ColumnDefinitions>
                                                <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" VerticalAlignment="Center" HorizontalAlignment="Left" RecognizesAccessKey="True"/>
                                                <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" Fill="{DynamicResource Muted}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource Primary}"/></Trigger></ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <TextBlock x:Name="SelectionText" Margin="11,0,32,0" VerticalAlignment="Center" Foreground="{DynamicResource Ink}" TextTrimming="CharacterEllipsis" IsHitTestVisible="False">
                                <TextBlock.Text><MultiBinding Converter="{StaticResource ComboSelectionTextConverter}"><Binding Path="SelectedItem" RelativeSource="{RelativeSource TemplatedParent}"/><Binding Path="DisplayMemberPath" RelativeSource="{RelativeSource TemplatedParent}"/></MultiBinding></TextBlock.Text>
                            </TextBlock>
                            <TextBox x:Name="PART_EditableTextBox"
                                     Style="{x:Null}"
                                     Margin="11,0,32,0"
                                     Padding="0"
                                     MinHeight="0"
                                     VerticalAlignment="Center"
                                     Background="Transparent"
                                     Foreground="{DynamicResource Ink}"
                                     CaretBrush="{DynamicResource Primary}"
                                     BorderThickness="0"
                                     FocusVisualStyle="{x:Null}"
                                     AutomationProperties.Name="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(AutomationProperties.Name)}"
                                     Text="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Visibility="Collapsed"/>
                            <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                <Border Background="{DynamicResource SurfaceElevated}" BorderBrush="{DynamicResource LineStrong}" BorderThickness="1" CornerRadius="8" Margin="0,4,0,0" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" MaxHeight="{TemplateBinding MaxDropDownHeight}" Effect="{StaticResource CardShadow}">
                                    <ScrollViewer Margin="4" SnapsToDevicePixels="True"><ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/></ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.45"/></Trigger>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="SelectionText" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter TargetName="DropDownToggle" Property="BorderBrush" Value="{DynamicResource Primary}"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/><Setter Property="Foreground" Value="{DynamicResource Ink}"/><Setter Property="Padding" Value="10,8"/><Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{DynamicResource PrimarySurface}"/></Trigger><Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{DynamicResource PrimarySoft}"/><Setter Property="Foreground" Value="{DynamicResource Ink}"/></Trigger></Style.Triggers>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource InkSecondary}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{DynamicResource Surface}"/>
            <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Line}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource Line}"/>
            <Setter Property="RowBackground" Value="{DynamicResource Surface}"/>
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource SurfaceMuted}"/>
            <Setter Property="RowHeight" Value="46"/>
            <Setter Property="ColumnHeaderHeight" Value="42"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="EnableRowVirtualization" Value="True"/>
            <Setter Property="EnableColumnVirtualization" Value="True"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{DynamicResource PrimarySurface}"/></Trigger>
                <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{DynamicResource PrimarySoft}"/><Setter Property="Foreground" Value="{DynamicResource Ink}"/></Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{DynamicResource SurfaceMuted}"/>
            <Setter Property="Foreground" Value="{DynamicResource Muted}"/>
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,11"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Line}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>
        <Style TargetType="ListBox"><Setter Property="Background" Value="{DynamicResource Surface}"/><Setter Property="Foreground" Value="{DynamicResource Ink}"/><Setter Property="BorderBrush" Value="{DynamicResource Line}"/></Style>
        <Style x:Key="PassivePageViewport" TargetType="ScrollViewer">
            <!-- Page-level scrolling is a passive viewport. Keep focus on its actionable descendants
                 instead of drawing WPF's default dotted rectangle around the whole product canvas. -->
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{DynamicResource SurfaceMuted}"/></Trigger>
                <Trigger Property="IsSelected" Value="True"><Setter Property="Background" Value="{DynamicResource PrimarySoft}"/></Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TabControl"><Setter Property="Background" Value="Transparent"/><Setter Property="BorderThickness" Value="0"/></Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{DynamicResource Muted}"/><Setter Property="Background" Value="Transparent"/><Setter Property="Padding" Value="14,9"/><Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="TabItem"><Border x:Name="TabRoot" Background="{TemplateBinding Background}" BorderBrush="{DynamicResource Line}" BorderThickness="1" CornerRadius="8,8,0,0" Padding="{TemplateBinding Padding}" Margin="0,0,4,0"><ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="TabRoot" Property="Background" Value="{DynamicResource SurfaceMuted}"/></Trigger><Trigger Property="IsSelected" Value="True"><Setter TargetName="TabRoot" Property="Background" Value="{DynamicResource PrimarySoft}"/><Setter Property="Foreground" Value="{DynamicResource Primary}"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
            <Style.Triggers><Trigger Property="IsSelected" Value="True"><Setter Property="Foreground" Value="{DynamicResource Primary}"/><Setter Property="Background" Value="{DynamicResource PrimarySoft}"/></Trigger></Style.Triggers>
        </Style>
        <Style TargetType="ProgressBar">
            <Setter Property="Foreground" Value="{DynamicResource Primary}"/>
            <Setter Property="Background" Value="{DynamicResource CanvasDeep}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="MinHeight" Value="6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="PART_Track" ClipToBounds="True">
                            <Border Background="{TemplateBinding Background}" CornerRadius="4"/>
                            <Border x:Name="PART_Indicator" Background="{TemplateBinding Foreground}" CornerRadius="4" HorizontalAlignment="Left"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ConfidenceMeter" TargetType="ProgressBar" BasedOn="{StaticResource {x:Type ProgressBar}}">
            <Setter Property="Foreground" Value="{DynamicResource AiAccent}"/>
            <Setter Property="Background" Value="{DynamicResource AiSoft}"/>
            <Setter Property="Height" Value="7"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="PageEyebrow" TargetType="TextBlock"><Setter Property="FontSize" Value="11"/><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Foreground" Value="{DynamicResource AiAccent}"/></Style>
        <Style x:Key="PageTitle" TargetType="TextBlock"><Setter Property="FontSize" Value="28"/><Setter Property="FontWeight" Value="Bold"/><Setter Property="Foreground" Value="{DynamicResource Ink}"/><Setter Property="LineHeight" Value="38"/></Style>
        <Style x:Key="SectionTitle" TargetType="TextBlock"><Setter Property="FontSize" Value="18"/><Setter Property="FontWeight" Value="SemiBold"/><Setter Property="Foreground" Value="{DynamicResource Ink}"/><Setter Property="LineHeight" Value="28"/></Style>
        <Style x:Key="BodyText" TargetType="TextBlock"><Setter Property="FontSize" Value="13"/><Setter Property="LineHeight" Value="21"/><Setter Property="Foreground" Value="{DynamicResource InkSecondary}"/></Style>
        <Style x:Key="LabelText" TargetType="TextBlock"><Setter Property="FontSize" Value="12"/><Setter Property="LineHeight" Value="18"/><Setter Property="Foreground" Value="{DynamicResource Muted}"/></Style>
        <Style x:Key="MicroText" TargetType="TextBlock"><Setter Property="FontSize" Value="11"/><Setter Property="LineHeight" Value="16"/><Setter Property="Foreground" Value="{DynamicResource Muted}"/></Style>
        <Style x:Key="MetricValue" TargetType="TextBlock"><Setter Property="FontSize" Value="30"/><Setter Property="FontWeight" Value="Bold"/><Setter Property="Foreground" Value="{DynamicResource Ink}"/><Setter Property="FontFamily" Value="Segoe UI Variable Display, Segoe UI"/></Style>
        <Style x:Key="PillText" TargetType="TextBlock"><Setter Property="FontSize" Value="11"/><Setter Property="FontWeight" Value="SemiBold"/></Style>
        <Style x:Key="NavIconFrame" TargetType="Viewbox">
            <Setter Property="Width" Value="18"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="Stretch" Value="Uniform"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>
        <Style x:Key="NavIconPath" TargetType="Path">
            <Setter Property="Fill" Value="Transparent"/>
            <Setter Property="Stroke" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"/>
            <Setter Property="StrokeThickness" Value="2"/>
            <Setter Property="StrokeStartLineCap" Value="Round"/>
            <Setter Property="StrokeEndLineCap" Value="Round"/>
            <Setter Property="StrokeLineJoin" Value="Round"/>
        </Style>
        <Style x:Key="NavIconRectangle" TargetType="Rectangle">
            <Setter Property="Fill" Value="Transparent"/>
            <Setter Property="Stroke" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"/>
            <Setter Property="StrokeThickness" Value="2"/>
            <Setter Property="StrokeLineJoin" Value="Round"/>
        </Style>
        <Style x:Key="NavIconEllipse" TargetType="Ellipse">
            <Setter Property="Fill" Value="Transparent"/>
            <Setter Property="Stroke" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"/>
            <Setter Property="StrokeThickness" Value="2"/>
        </Style>
        <Style x:Key="NavIconDot" TargetType="Ellipse">
            <Setter Property="Width" Value="2"/>
            <Setter Property="Height" Value="2"/>
            <Setter Property="Fill" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"/>
        </Style>
        <Style x:Key="NavText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="Tag" Value="SidebarReveal"/>
            <Setter Property="RenderTransform">
                <Setter.Value><TranslateTransform X="-6"/></Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SidebarSectionLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource SidebarMuted}"/>
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="Tag" Value="SidebarReveal"/>
            <Setter Property="RenderTransform">
                <Setter.Value><TranslateTransform X="-6"/></Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/><Setter Property="Foreground" Value="{DynamicResource SidebarText}"/><Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/><Setter Property="HorizontalContentAlignment" Value="Left"/><Setter Property="Padding" Value="13,12"/><Setter Property="Margin" Value="0,2"/><Setter Property="FontSize" Value="12.5"/><Setter Property="FontWeight" Value="Medium"/><Setter Property="Cursor" Value="Hand"/><Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="local:MotionAssist.IsEnabled" Value="True"/>
            <Setter Property="local:MotionAssist.HoverScale" Value="1"/>
            <Setter Property="local:MotionAssist.PressedScale" Value="0.985"/>
            <Setter Property="local:MotionAssist.HoverOffsetX" Value="2"/>
            <Setter Property="local:MotionAssist.HoverOffsetY" Value="0"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
                <Grid ClipToBounds="True">
                    <Border x:Name="HoverSurface" Background="{DynamicResource SidebarHover}" CornerRadius="10" Opacity="0"/>
                    <Border x:Name="FocusSurface" Background="{DynamicResource SidebarHover}" CornerRadius="10" Opacity="0"/>
                    <Border x:Name="ActiveSurface" Background="{DynamicResource SidebarActive}" CornerRadius="10" Opacity="0"/>
                    <Border x:Name="NavRoot" Background="Transparent" CornerRadius="10" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    </Border>
                    <Border x:Name="ActiveRail" Width="3" Height="22" CornerRadius="2" Background="{DynamicResource Primary}" HorizontalAlignment="Left" VerticalAlignment="Center" Opacity="0" RenderTransformOrigin="0.5,0.5">
                        <Border.RenderTransform><ScaleTransform ScaleY="0.35"/></Border.RenderTransform>
                    </Border>
                    <Border x:Name="FocusRail" Width="3" Height="22" CornerRadius="2" Background="{DynamicResource Primary}" HorizontalAlignment="Left" VerticalAlignment="Center" Opacity="0"/>
                </Grid>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Foreground" Value="{DynamicResource SidebarText}"/>
                        <Trigger.EnterActions>
                            <BeginStoryboard HandoffBehavior="SnapshotAndReplace">
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="HoverSurface" Storyboard.TargetProperty="Opacity" To="0.82" Duration="0:0:0.16">
                                        <DoubleAnimation.EasingFunction><SineEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.EnterActions>
                        <Trigger.ExitActions>
                            <BeginStoryboard HandoffBehavior="SnapshotAndReplace">
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="HoverSurface" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.20">
                                        <DoubleAnimation.EasingFunction><SineEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.ExitActions>
                    </Trigger>
                    <Trigger Property="local:MotionAssist.IsSelected" Value="True">
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Trigger.EnterActions>
                            <BeginStoryboard HandoffBehavior="SnapshotAndReplace">
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="ActiveSurface" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.22">
                                        <DoubleAnimation.EasingFunction><SineEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                    <DoubleAnimation Storyboard.TargetName="ActiveRail" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.18"/>
                                    <DoubleAnimation Storyboard.TargetName="ActiveRail" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.22">
                                        <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.EnterActions>
                        <Trigger.ExitActions>
                            <BeginStoryboard HandoffBehavior="SnapshotAndReplace">
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="ActiveSurface" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.14"/>
                                    <DoubleAnimation Storyboard.TargetName="ActiveRail" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.12"/>
                                    <DoubleAnimation Storyboard.TargetName="ActiveRail" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="0.35" Duration="0:0:0.14"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.ExitActions>
                    </Trigger>
                    <Trigger Property="IsKeyboardFocused" Value="True">
                        <Trigger.EnterActions>
                            <BeginStoryboard HandoffBehavior="SnapshotAndReplace">
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="FocusSurface" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.12"/>
                                    <DoubleAnimation Storyboard.TargetName="FocusRail" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.12"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.EnterActions>
                        <Trigger.ExitActions>
                            <BeginStoryboard HandoffBehavior="SnapshotAndReplace">
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="FocusSurface" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.12"/>
                                    <DoubleAnimation Storyboard.TargetName="FocusRail" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.12"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.ExitActions>
                    </Trigger>
                    <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.42"/><Setter Property="Cursor" Value="Arrow"/></Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>
    </Application.Resources>
</Application>
