# Relvyn 隐私说明（项目草案）

版本：1.0-draft（2026-08-04）

> 本说明依据当前仓库代码和构建方式编写，尚未经律师审查。它说明软件如何工作，不代替用户所在地要求的隐私告知、数据处理协议或同意机制。

## 1. 本地优先的数据位置

Relvyn 默认使用 `%LOCALAPPDATA%\WAFlow` 作为业务数据工作区。用户可以在“设置 → 本地数据工作区”迁移到其他本机固定磁盘。升级改名后继续识别既有工作区和旧版定位文件，不自动移动或删除业务数据。

工作区可以包含：

- SQLite 客户、商机、自动化、消息索引、邮件索引、设置和分析记录；
- WhatsApp 加密会话文件、已同步消息和媒体；
- Knowledge Base 原件、索引和提取结果；
- 客户分析报告、导出文件、备份及恢复档案；
- 更新或迁移所需的本地状态文件。

具体目录会随用户选择的工作区变化。软件卸载不应被理解为自动删除这些业务数据。

## 2. 凭据

AI Provider、搜索 Provider、邮箱及其他受支持凭据按当前 Windows 实现写入 Windows 凭据管理器，使用历史兼容的 `WAFlow/...` 目标名称；不会以明文写入 README 或发布包。macOS 预览版使用系统 Keychain。用户输入的 API Key 会在验证或调用时发送给相应 Provider。

## 3. 默认不会上传给开发者的内容

当前代码未发现开发者自建的客户数据接收服务器、广告 SDK、行为分析 SDK、自动崩溃上报服务或默认遥测管道。客户数据库、WhatsApp 会话、邮件内容、知识库和报告不会仅因启动软件而上传到开发者服务器。

这不等于软件“绝不联网”。用户启用下列功能时，数据会直接流向用户选择或功能必需的第三方服务。

## 4. AI Provider 数据流

启用任意 AI Provider 时，请求可能包括用户提示词，以及完成所选功能所需的客户字段、人工备注、WhatsApp 或邮件上下文、Knowledge Base 片段、公开来源、分析中间结果和输出格式要求。请求发送到用户选择的 Provider 和 Base URL。

软件不能控制 Provider 的日志、保留、训练、跨境传输或安全措施。用户必须阅读并接受相应 Provider 的条款和隐私政策，并确认有合法依据向其发送相关数据。

## 5. 搜索和公开网页数据流

启用 Tavily 或 Brave Search 时，软件会向相应 API 发送为客户外部调查生成的搜索关键词、语言和结果数量等请求参数。启用本机 SearXNG 时，请求发送到用户配置的 SearXNG 地址。读取公开网页时，目标网站会接收到普通网络请求信息，例如出口 IP、请求头和目标 URL。

## 6. 邮箱数据流

邮箱功能使用用户配置的 IMAP/SMTP 服务同步和发送邮件。邮箱地址、凭据、邮件头、正文和附件在用户设备与邮箱服务商之间传输；本地索引和会话记录写入用户工作区。服务商的保留和安全政策由用户与服务商之间的协议决定。

## 7. WhatsApp Bridge

Windows 发布包包含本地伴随进程 `Relvyn.WhatsApp.Bridge.exe`。桌面程序在本机启动该进程并通过本地标准输入/输出交换命令和事件；Bridge 随后连接 WhatsApp Web 多设备服务以扫码、同步和收发数据。登录会话保存在本地工作区。此实现不是 Meta 或 WhatsApp 官方接口，不能承诺持续兼容、账号安全或端到端处理属性超出第三方协议本身的保证。

## 8. 网络位置与更新

公网 IP 提醒会向 ipify 查询出口 IP；首次检测或 IP 变化时会向 ipwho.is 查询近似国家、地区和城市。请求会暴露用户的公网 IP，但不包含客户数据库或消息正文。

自动更新会访问公开 GitHub Release 资源以读取更新清单、版本说明和安装包。下载完成后，只有用户确认才安装并重启。GitHub 会按其政策处理常规请求信息。

## 9. 日志、崩溃和遥测

当前实现会在本机进程输出、调试输出和本地工作区中产生诊断信息；Bridge 错误也可能被桌面进程记录。当前未发现自动向开发者发送日志或崩溃数据的实现。用户在提交支持材料前应检查并移除客户内容、邮箱地址、电话号码、路径、API 返回和其他敏感信息。

## 10. 删除与保留

用户可以在停止 Relvyn 及相关 Bridge 进程并完成所需备份后：

1. 删除设置中显示的本地数据工作区，以移除客户数据库、消息、媒体、知识库、报告和本地日志；
2. 在 WhatsApp 页面退出账号，并按需删除工作区内遗留的会话备份；
3. 在 Windows 凭据管理器中删除名称以 `WAFlow/` 开头且确认属于 Relvyn 的凭据；
4. 删除 `%LOCALAPPDATA%\Relvyn` 或旧版 `%LOCALAPPDATA%\AI Sales OS` 中的工作区定位和迁移状态（如存在）；
5. 删除 Velopack/Relvyn 识别的安装与更新缓存。

删除前必须确认路径。软件的卸载流程当前不保证清除工作区、凭据或备份。发送到第三方 Provider 的数据无法由 Relvyn 在本地删除；用户应向相应 Provider 行使删除或访问权利。

## 11. 用户责任与联系

用户是其客户数据、营销名单和消息内容的责任方，必须确保收集、导入、使用、共享、保留和删除具有合法依据，并响应数据主体请求。

隐私联系邮箱：[TO BE CONFIRMED]
运营或许可主体：[TO BE CONFIRMED]
适用地区和跨境处理安排：[TO BE CONFIRMED]

第三方组件的许可证与版权说明见 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。最终销售前应由熟悉目标市场隐私与软件许可法律的专业人士复核本说明。

